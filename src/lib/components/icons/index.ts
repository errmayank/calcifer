import CalciferIcon from "./calcifer.svelte";
import CheckIcon from "./check.svelte";

export { CalciferIcon, CheckIcon };

export type IconProps = {
    size: number;
    color?: string;
    class?: string;
};
